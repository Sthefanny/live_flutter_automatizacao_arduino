import 'package:flutter_test/flutter_test.dart';

import 'package:flutter_arduino/app/shared/services/light_service_service.dart';

void main() {
  LightService service;

  setUp(() {
//    service = LightServiceService();
  });
//
  group('LightServiceService Test', () {
//    test("First Test", () {
//      expect(service, isInstanceOf<LightServiceService>());
//    });
//
  });
}
